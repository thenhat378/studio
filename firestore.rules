
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Cho phép đọc và ghi dữ liệu công khai để đảm bảo tốc độ và sự tiện lợi trong giai đoạn này
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
